import element from '/js/lib/helpers/elements.js';

const {
   GEBI,
   // GEBCN,
} = element; 

const 
   canvas = GEBI('canvas'),
   main_panel = GEBI('main-panel'); 

const DOM = {
   canvas,
   main_panel,
};

export default DOM;